# ObjectTracking

This repository depends on [TransReID](https://github.com/anuj018/TransReID), which is included as a **git submodule**.

## Clone

When cloning this repository, make sure to pull submodules as well:

```bash
# One-shot clone with submodules
git clone --recurse-submodules git@github.com:firas-genfied/python_scripts_tests.git
cd python_scripts_tests
